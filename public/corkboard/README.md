Photos for the corkboard wall.

Drop image files here, then add them to a slot's `photos` array in
`src/data/corkboard.ts`:

    photos: [{ image: "/corkboard/hackathon-01.jpg", caption: "..." }],

The first photo in a slot is the thumbnail pinned on the 3D board.
