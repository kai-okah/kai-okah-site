Certificate scans for the dossier on the desk.

Drop image files here (jpg/png, ~1200px wide is plenty), then add the
filename to the matching entry in `src/data/certificates.ts`:

    image: "/certificates/cs50x.jpg",
